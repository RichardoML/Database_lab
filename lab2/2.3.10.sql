SELECT BID,COUNT(*) AS NUM 
FROM TOPDAY
WHERE TOPDAY.BID IN 
(
/*
	2000后点赞>1的记录
*/
SELECT BID
FROM THUMB 
WHERE [UID] IN
	(
	/*
	2000后出生的用户
	*/
	SELECT [UID]
	FROM [USER]
	WHERE BYEAR > 2000
	)
	GROUP BY BID
	HAVING COUNT(*) >1
)
GROUP BY TOPDAY.BID 