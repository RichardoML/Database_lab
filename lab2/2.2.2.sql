CREATE TABLE FANS_3
(
	UID INT,
	NAME VARCHAR(64),
	SEX CHAR(2),
	BYEAR INT,
	CITY VARCHAR(64),
	PRIMARY KEY(UID) 
);

INSERT INTO FANS_3
SELECT * 
FROM [USER]
WHERE	UID	 IN	
	(SELECT UID
	FROM FOLLOW
	WHERE UIDFLED = 3)
